<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metropolis.dal.persistence.SysUserMapper">

    <resultMap type="com.metropolis.user.entity.SysUser" id="SysUserMap">
        <result property="id" column="id" jdbcType="BIGINT"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="account" column="account" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="salt" column="salt" jdbcType="VARCHAR"/>
        <result property="locked" column="locked" jdbcType="BOOLEAN"/>
        <result property="createtime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="lastLoginTime" column="last_login_time" jdbcType="TIMESTAMP"/>
        <result property="isDelete" column="is_delete" jdbcType="BOOLEAN"/>
    </resultMap>

    <!--查询单个-->
    <select id="getUserById" resultMap="SysUserMap" >
        select
          id, username, locked, create_time, last_login_time, is_delete
        from metropolis.sys_user
        where id = #{id}
    </select>

    <select id="getUserByName" resultMap="SysUserMap">
        select
          id,account, username, password, salt, locked, create_time, last_login_time, is_delete
        from metropolis.sys_user
        where username = #{name}
    </select>

    <select id="getUserByAccount" resultMap="SysUserMap">
        select
          id,account, username, password, salt, locked, create_time, last_login_time, is_delete
        from metropolis.sys_user
        where account = #{account}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="query" resultMap="SysUserMap">
        select
          id,account, username, password, salt, locked, create_time, last_login_time, is_delete
        from sys_user
        <where>
            <if test="user.id != null">
                and id = #{user.id}
            </if>
            <if test="user.account != null and user.account != ''">
                and account = #{user.account}
            </if>
            <if test="user.username != null and user.username != ''">
                and username = #{user.username}
            </if>
            <if test="user.password != null and user.password != ''">
                and password = #{user.password}
            </if>
            <if test="user.salt != null and user.salt != ''">
                and salt = #{user.salt}
            </if>
            <if test="user.locked != null">
                and locked = #{user.locked}
            </if>
            <if test="user.createtime != null">
                and createtime = #{user.createtime}
            </if>
            <if test="user.lastLoginTime != null">
                and last_login_time = #{user.lastLoginTime}
            </if>
            <if test="user.isDelete != null">
                and is_delete = #{user.isDelete}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="save" keyProperty="id" useGeneratedKeys="true">
        insert into metropolis.sys_user(account,username, password, salt, locked, create_time, last_login_time, is_delete)
        values (#{user.account},#{user.username}, #{user.password}, #{user.salt}, #{user.locked}, #{user.createtime}, #{user.lastLoginTime}, #{user.isDelete})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update metropolis.sys_user
        <set>
            <if test="user.username != null and user.username != ''">
                username = #{user.username},
            </if>
            <if test="user.account != null and user.account != ''">
                account = #{user.account},
            </if>
            <if test="user.password != null and user.password != ''">
                password = #{user.password},
            </if>
            <if test="user.salt != null and user.salt != ''">
                salt = #{user.salt},
            </if>
            <if test="user.locked != null">
                locked = #{user.locked},
            </if>
            <if test="user.createtime != null">
                create_time = #{user.createtime},
            </if>
            <if test="user.lastLoginTime != null">
                last_login_time = #{user.lastLoginTime},
            </if>
            <if test="user.isDelete != null">
                is_delete = #{user.isDelete},
            </if>
        </set>
        <where>
            <if test="user.id != null">
              id = #{user.id}
            </if>
        </where>
    </update>

    <!--通过主键删除-->
    <delete id="delete">
        delete from metropolis.sys_user where id = #{id}
    </delete>

    <delete id="batchDelete">
        delete from metropolis.sys_user where id in
        <foreach collection="ids" item="id" index="i" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>